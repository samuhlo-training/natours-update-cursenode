meta {
  name: Update Photo Test
  type: http
  seq: 6
}

post {
  url: {{base_url}}/users/uploadPhotoTest
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:multipart-form {
  photo: @file(/home/samu/Documentos/lemon.jpeg)
}
