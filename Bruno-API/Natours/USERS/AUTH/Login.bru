meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/users/login
  body: json
  auth: none
}

body:json {
  {
      "email": "laura@example.com",
      "password": "{{password}}"
      }
}

tests {
  bru.setEnvVar("jwt",res.getBody().token);
}
