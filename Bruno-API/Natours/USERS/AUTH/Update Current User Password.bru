meta {
  name: Update Current User Password
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/users/updateMyPassword
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
      "passwordCurrent" :"password123",
      "password": "password1234",
      "passwordConfirm":"password1234"
  }
}

tests {
  bru.setEnvVar("jwt",res.getBody().token);
}
