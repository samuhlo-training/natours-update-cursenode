meta {
  name: Update User
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/users/3
  body: json
  auth: none
}

body:json {
  {
      "duration" : 15
  }
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
