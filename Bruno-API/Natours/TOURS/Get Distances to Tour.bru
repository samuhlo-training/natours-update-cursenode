meta {
  name: Get Distances to Tour
  type: http
  seq: 4
}

get {
  url: {{base_url}}/tours/distances/34.111745,-118.113491/unit/km
  body: none
  auth: none
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
